This is an Express and React project that uses the lichess API to fetch game history.

Start the Express endpoint with `yarn start` in the home directory,
and start the client by ./client and then `yarn start`.

When you're ready to deploy, `yarn build` in ./client.